# HashMap类

`get`方法不再直接对不存在的值返回初始化对象，而是通过`Pair<bool, T2>`返回。

通过`->first`判断查询的对象是否存在。

# define

- 时间倍速

# Time
in utils.h.

timestamp - int

toString - format time

incre(x) - up x hours. (Like desc)

